// Note: this namespace used to back a "RoleScreen" (device role picker)
// that was removed as unreachable dead code — only the pairing-code strings
// below are still used, by PairingCodeModal/PairingQrScanner.
export const role = {
  enterPairingCode: 'Enter the 6-character code from the child device.',
  unableToJoinWithCode: 'That code is incorrect or has expired.',
  connecting: 'Connecting…',
};
